# docker-compose.yml
version: '3.11'

services:
  serving:
    build:
      context: ./
      dockerfile: ./Dockerfile.serving
    image: flaskp-app
    ports:
        - 5000:8080
    environment:
        - COMET_API_KEY=${COMET_API_KEY}